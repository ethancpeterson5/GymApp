import React from 'react';
import { View, Text } from 'react-native';

export default function ResetPasswordWithPhoneScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Add functionality to reset password with Phone number</Text>
      </View>
  );
}
